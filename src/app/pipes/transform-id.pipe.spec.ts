import { TransformIdPipe } from './transform-id.pipe';

describe('TransformIdPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformIdPipe();
    expect(pipe).toBeTruthy();
  });
});
